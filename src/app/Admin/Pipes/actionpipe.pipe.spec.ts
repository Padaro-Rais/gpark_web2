import { ActionpipePipe } from './actionpipe.pipe';

describe('ActionpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ActionpipePipe();
    expect(pipe).toBeTruthy();
  });
});
